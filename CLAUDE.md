# HIJ AI/자동화 진행현황 대시보드

PT HANSOLL INDO JAVA(HIJ)의 부서별 AI/자동화 과제 진행현황 대시보드.

## 프로젝트 성격

**단일 파일 정적 HTML 앱.** 빌드 도구·패키지 의존성·테스트 스위트가 없다.
HTML/CSS/JS가 한 파일에 전부 들어 있고, 브라우저로 파일을 열면 바로 동작한다.

- `npm install`, 번들러, 개발 서버 불필요
- 외부 리소스는 CDN 2건뿐: Pretendard 폰트(jsdelivr), IBM Plex Mono(Google Fonts)

## 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | **정규 파일. 모든 수정은 여기에 한다.** |
| `HIJ_AI_자동화_진행현황.html` | 초기 업로드본(아카이브). ME 부서 7건 데이터가 빠진 구버전. |

두 파일은 현재 약 50줄 차이가 난다. **둘을 동시에 고치려 하지 말 것** —
`index.html`만 정규본으로 관리하고, 아카이브본은 건드리지 않는다.

## 코드 구조 (index.html)

데이터 시드와 상수는 `<script>` 블록 상단에 모여 있다:

- `DEFAULT_DEPARTMENTS` — 부서 목록 (ME, MD/PPIC, SAMPLE, CSR, EXIM, ACCOUNTING, HRD, QA)
- `STAGES` — 진행 단계 5종 (기획/검토 → 초기 도입 → 파일럿 테스트 → 안정화 단계 → 완료/운영)
- `DEPT_COLORS` — 부서별 차트 색상
- `SAMPLE_PROJECTS` — 과제 시드 데이터 (`id`, `dept`, `category`, `title`, `stage`, `achv`, `goal`, `link`, `createdAt`)
- `SAMPLE_KPIS` — KPI 시드 데이터
- `STORAGE_KEYS` / `saveProjects()` 등 — localStorage 영속화 계층

과제 데이터를 추가·수정할 때는 `SAMPLE_PROJECTS` 배열을 편집한다.

## ⚠️ 데이터는 기기 간 공유되지 않는다

이 대시보드는 **브라우저 localStorage에 데이터를 저장**한다. 서버 DB가 없다.

결과적으로 데스크탑1 / 데스크탑2 / 핸드폰이 **각각 완전히 별개의 데이터**를 갖는다.
한 기기에서 과제를 등록해도 다른 기기에는 나타나지 않는다. 브라우저 캐시를 지우면
그 기기의 입력분은 사라진다.

기기 간에 데이터를 맞추려면 둘 중 하나:

1. **CSV 내보내기 → 다른 기기에서 가져오기** (앱 내 기능)
2. **`SAMPLE_PROJECTS` 시드를 직접 수정해 커밋** — 이게 영구 보존되는 유일한 경로다.
   단, 이미 localStorage에 값이 있는 브라우저는 시드를 다시 읽지 않으므로
   해당 기기에서 저장소를 비워야 새 시드가 반영된다.

**중요한 데이터는 반드시 시드에 반영해 커밋할 것.** localStorage에만 있는 값은
백업되지 않으며 기기 이동 시 유실된다.

## 기기 간 작업 인계 규칙

데스크탑1·데스크탑2·핸드폰을 오가며 작업하므로, 아래를 지켜야 작업이 엇갈리지 않는다.

**작업 시작 전 — 항상 최신 상태부터 확인**
```bash
git fetch origin && git status
git pull origin <작업-브랜치>
```

**작업 종료 시 — 커밋하지 않은 변경은 그 기기에만 남는다**
```bash
git add -A && git commit -m "<변경 내용>"
git push -u origin <작업-브랜치>
```

작업을 중간에 멈추고 다른 기기로 넘어갈 때도 **반드시 푸시한다.**
푸시하지 않은 변경은 다른 기기·클라우드 세션에서 절대 보이지 않는다.

**세션 종류별 공유 범위**

| 방식 | 대화 기록 | 파일 변경 |
|---|---|---|
| 클라우드 세션 (claude.ai/code, 모바일 앱) | 계정 단위로 전 기기 공유 | 푸시해야 공유 |
| 로컬 터미널 CLI (`claude`) | 그 PC에만 저장 (`~/.claude/projects/`) | 푸시해야 공유 |
| Remote Control | 원본 PC가 켜져 있어야 접속 가능 | 푸시해야 공유 |

어떤 방식이든 **파일 변경의 공유 수단은 git 푸시 하나뿐**이다.

## 자동화된 동기화 장치

기기 간 작업이 흩어지지 않도록 두 가지가 자동으로 돌아간다.

### 1. 세션 시작 훅 — 기기 환경 자동 기록

`.claude/hooks/session-start.sh` 가 **모든 기기의 세션 시작 시** 실행된다
(클라우드 세션·로컬 CLI 양쪽 모두).

하는 일:

- 브랜치, 원격 대비 ahead/behind, 미커밋 변경, 마지막 커밋을 출력한다
- 이 기기의 환경을 `.claude/devices/<기기ID>.md` 에 기록한다 —
  OS, 저장소 경로, 세션 종류, 설치된 도구 버전, 추적 파일 목록,
  이 기기에만 있는 미추적 파일
- 다른 기기들의 마지막 접속 시각을 요약해 보여준다

기기 ID는 클라우드 세션이면 `cloud-session`, 로컬이면 호스트명이다.

**스냅샷은 커밋해야 다른 기기에서 보인다.** 훅은 파일을 쓰기만 하고 커밋하지
않는다(작업 중에 임의로 커밋을 만들지 않기 위해서). `.claude/devices/` 아래
변경이 있으면 다음 커밋에 함께 포함시킨다.

`.claude/devices/*.md` 는 자동 생성 파일이므로 직접 편집하지 않는다.

### 2. 일일 진행 리포트 — 매일 오후 5시 (WIB)

매일 17:00 자카르타 시간(10:00 UTC)에 Routine이 새 세션을 띄워
하루치 진행 내역을 정리하고, 푸시 알림과 이메일로 보낸다.

보고 내용: 기기별 오늘의 작업, 미병합 브랜치·충돌 위험, 기기별 접속 현황,
데이터 시드 변경 여부.

**읽기 전용이다.** 이 리포트는 코드를 고치거나 커밋하지 않는다.

리포트에 잡히려면 **그날 작업을 푸시해야 한다.** 로컬에만 있는 커밋은
집계되지 않는다.

## 새 기기 연결하기

새 랩탑·데스크탑을 이 저장소의 기기 동기화에 참여시킬 때의 절차.

**1. 저장소를 받는다**

```bash
git clone https://github.com/nixair-VSC/hij-dashboard
cd hij-dashboard
```

동기화 설정이 아직 `main`에 병합되지 않았다면 해당 브랜치를 체크아웃한다:

```bash
git checkout claude/claudecode-desktop-mobile-sharing-9j31r0
```

**2. 기기를 등록한다**

```bash
bash .claude/connect-device.sh
```

스냅샷 생성 → 커밋 → 푸시를 한 번에 처리한다. 소스 코드는 건드리지 않고
`.claude/devices/` 아래만 커밋한다.

**3. 확인한다**

이후 이 기기에서 Claude Code를 실행하면 세션 시작 시 다른 기기 목록이 뜬다.

도구를 새로 설치했거나 저장소 경로가 바뀌는 등 환경이 달라지면 같은 명령을
다시 실행해 기록을 갱신한다.

## 편집 규칙

- `index.html`은 10만 자가 넘는 단일 파일이다. **부분 수정(Edit/sed)으로 작업하고,
  파일 전체를 재생성하지 않는다.** 전체 재작성은 무관한 부분을 훼손할 위험이 크다.
- **UTF-8 인코딩을 유지한다.** 한국어 콘텐츠가 전부이므로 인코딩이 깨지면 복구가 번거롭다.
- UI 문구·주석·커밋 메시지는 한국어로 작성한다.
- 색상·간격은 `:root`의 CSS 변수를 쓴다. 하드코딩된 색상값을 새로 넣지 않는다.

## 확인 방법

빌드가 없으므로 브라우저로 직접 연다:

```bash
python3 -m http.server 8000   # http://localhost:8000/index.html
```

자동화된 테스트·린터는 없다. 변경 후에는 브라우저에서 해당 화면을 눈으로 확인한다.

## 보안 메모

`index.html`의 계정 배열에 관리자 계정과 비밀번호가 **평문으로 하드코딩**되어 있다.
클라이언트 전용 앱이라 브라우저에서 누구나 열람 가능하다. 사내망 밖에 공개 배포할
계획이 생기면 인증 방식부터 재설계해야 한다.
